export * from './specialist-loader.js';
export * from './config-bridge.js';
// Removed: handoff-service.ts (handoffs show buttons after EVERY message - too noisy)
// Removed: workflow-session.ts (status bar removed - no sync between chat UI and extension UI possible)
